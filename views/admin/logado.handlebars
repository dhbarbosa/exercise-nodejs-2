<main>

    <div class='p-5 mb-4 bg-light rounded-3'>

        <div class='container-sm py-5'>

            <div class='rounded'>

                <div class='p-5 pt-0'>
                    {{#each nprofessor}}
                        <h1>Bem vindo!</h1>{{dataValues.nome}}
                    {{/each}}
                </div>
            </div>
        </div>

    </div>

</main>